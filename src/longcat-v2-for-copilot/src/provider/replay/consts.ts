/** MIME type used for replay marker data parts. */
export const REPLAY_MARKER_MIME = 'application/vnd.longcat.replay-marker+json';

/** Recognised marker writer prefixes. */
export const REPLAY_MARKER_WRITER_ID = 'longcat';

export const REPLAY_MARKER_PREFIXES = new Set(['longcat', 'deepseek']);
